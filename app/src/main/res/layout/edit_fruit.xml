<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:text="@string/enter_fruit_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/edit_fruit_text"
        android:textSize="24sp"
        android:textAlignment="center"
        android:paddingBottom="10dp"
        android:paddingTop="10dp"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:ems="10"
        android:id="@+id/edit_fruit_name_edit"
        android:textAlignment="center"
        android:textSize="24sp"
        android:paddingBottom="10dp"/>

    <Button
        android:text="@string/edit_fruit_text_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/edit_fruit_button_dialog"
        android:textSize="18sp"
        android:background="@color/floating_button"/>
</LinearLayout>